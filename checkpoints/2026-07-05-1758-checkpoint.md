---
checkpoint: 2026-07-05T17:58 local
project: modular-custom (NVFP4 sm_120)
cwd: /Users/mardel/src/opensource/modular/modular-custom
git_branch: feat/nvfp4-sm120-native-kernel
git_head: 4d72ac09e5 (local; max-build worktree carries the same uncommitted files)
---

# Checkpoint — D2/M2 complete: tiled W4A4 GEMM validated, wired, A/B'd vs bf16

## What we did this session
- **W4A4 framework numeric test PASSED** (`test_linear_nvfp4_cuda_w4a4_matches_sim`,
  target `//max/tests/integration/nn:cuda_nvfp4_tests -k w4a4`): native op ==
  in-graph fp4-act fake-quant + materialize oracle, through the real MAX graph
  compiler. Helpers `_snap_to_e2m1_graph` / `_fake_quant_fp4_act_graph` added to
  `max/tests/integration/nn/test_linear_nvfp4_cuda_gpu.py`.
- **True-FP4 Klein render validated**: served with `MODULAR_NVFP4_W4A4=1`, rendered
  4 hard prompts (seed 42, 1024²) → all clean; kernel confirmed active (w4a4-vs-w4a16
  MAE 3.86/21.50/12.07/4.98 ≠ 0). Images: max-build `/root/w4a4_*.png`, Mac
  `/Users/mardel/nvfp4-w4a4-renders/true/`.
- **compare.html upgraded to 4 variants** (bf16 / W4A16 / W4A4-sim / W4A4-native,
  keys 1–4): `/Users/mardel/nvfp4-w4a4-renders/compare.html`.
- **D2/M2 benchmark** (`max/kernels/benchmarks/gpu/linalg/bench_nvfp4_w4a4.mojo`,
  registered in `max/kernels/benchmarks/BUILD.bazel`): naive W4A4 GEMM flat ~29 TF/s,
  3.3–13.8× slower than bf16(flush) on real Klein shapes.
- **Built + validated the SMEM-tiled FP4×FP4 GEMM** `_gemm_kernel_tiled` /
  `nvfp4_w4a4_matmul_cuda_tiled` in
  `max/kernels/src/linalg/matmul/gpu/nvfp4_w4a4_cuda.mojo` (BM/BN/BK 64, 4 warps,
  tensor-indexed SMEM/acc). 3-way test `max/kernels/test/gpu/linalg/nvfp4_w4a4_tiled_test.mojo`:
  tiled ≡ naive bit-exact; both == device-quant host ref; quant byte-exact.
- **Root-caused the phantom "codegen bug"** → standalone-harness DeviceBuffer
  lifetime (ASAP-drop after `TileTensor(ptr)` → in-stream free before kernels →
  launcher transients clobber inputs). Also explains the pre-summary abandoned
  `nvfp4_w4a4_launch_test.mojo`.
- **Wired the op** `mo.matmul.block.scaled.cuda.w4a4` → tiled launcher
  (`max/kernels/src/graph_compiler/builtin_kernels/linalg.mojo`); framework test
  re-PASSED; re-render: **2 s/1024² (was 9–10 s), all 4 PNGs BYTE-IDENTICAL** to
  naive-kernel renders (backup `/root/w4a4_naive_kernel_renders/`).
- **Honest A/B** (scheduler-log ms): bf16 1.49 s vs true-FP4 1.96 s mean per 1024²
  render → **1.32× from parity** (was ~6.5×).

## Lessons learned (persisted)
- Harness DeviceBuffer lifetime → memory `mojo-devicebuffer-harness-lifetime`,
  ADR `plans/decisions/0004-w4a4-tiled-gemm-harness-lifetime.md`, and the test's
  module docstring.
- Kernel idioms (tensor-indexed SMEM/acc, validated inline-asm pattern,
  artifact-invariant debugging) → memory `mojo-gpu-kernel-idioms`.
- max-build ops: `bazelw run` (not `test`) for GPU mojo_test targets locally
  ("gpu-memory not tracked"); any `.mojo` in `test/gpu/linalg/` auto-globs into a
  target; `OneShotScheduler: (Starting|Completed)` grep = ms render timing →
  memory `max-build-box-nvfp4`.
- bash: `local a="$1" b="$2" c="...${a}"` in one statement trips `set -u`
  ("a: unbound variable") — split `local` declarations (bit the render driver).

## Decisions made
- **Op path = tiled kernel** (naive stays as the test oracle). ADR 0004.
- **#22 scoped**: cp.async double-buffer → 128-wide tiles + swizzle → quant-alloc
  hoist → small-M occupancy; correctness gate after each rung (3-way test +
  framework test). Rejected for now: wgmma/ldmatrix restructure (different MMA model).

## Current state — what's in flight
- Nothing running. GPU idle; all serves torn down. Benchmark/test logs on max-build:
  `/root/bench_w4a4.log`, `/root/tiled_test.log`, `/root/w4a4_tiled.status`,
  `/root/bf16timed.status`.
- Uncommitted on BOTH Mac and max-build (`/opt/modular-custom`, synced by scp):
  `nvfp4_w4a4_cuda.mojo` (naive+tiled), `builtin_kernels/linalg.mojo` (op→tiled),
  `nn/kernels.py` + `nn/quant_ops.py` (`_cuda_w4a4_matmul`, `MODULAR_NVFP4_W4A4`),
  `test_linear_nvfp4_cuda_gpu.py` (w4a4 test), `nvfp4_w4a4_tiled_test.mojo`,
  `bench_nvfp4_w4a4.mojo` + benchmarks/BUILD.bazel, plans/ docs. Dev-scratch tests
  (`nvfp4_gemm_m1*.mojo`, `nvfp4_ttptr_test.mojo`, `nvfp4_w4a4_launch_test.mojo`,
  etc.) are candidates for deletion before commit.
- Deploy (repack wheels with W4A4) NOT done — packaged wheels still carry the old
  materialize path.

## Open questions / things to revisit
- Can #22 rungs close the remaining 1.32× (≈0.47 s of quantized-GEMM time)?
  GEMM must go from 1.5–1.9× → <1.0× of bf16; cp.async + bigger tiles are the
  standard levers for ~1.5–2×.
- Small-M regression: tiled 2.1× slower than naive at (4096,4096) M=64 (grid
  underfill). Matters for small images / short text streams.
- Original user complaint was at 128² (M≈256): re-measure small-image renders
  after #22.
- Commit strategy: squash the W4A4 work into clean commits (kernel, op+python,
  tests, bench) before repack/deploy.

## How to resume
1. Read `plans/nvfp4-d2-m2-benchmark-optimize.md` (ladder + all numbers) and
   ADR 0004. Task list: #22 (optimize), #21 (deploy), #16/#17 (D1 leftovers).
2. Reach the box: `ssh root@max-build`, repo `/opt/modular-custom`, GPU must be
   free (`nvidia-smi`); if max-serve's bf16 service holds the shared GPU:
   `ssh root@max-serve systemctl stop max-serve`.
3. Iterate on the kernel (Mac edit → scp → run):
   `scp max/kernels/src/linalg/matmul/gpu/nvfp4_w4a4_cuda.mojo root@max-build:/opt/modular-custom/max/kernels/src/linalg/matmul/gpu/`
4. Correctness gate (after EVERY kernel change):
   `./bazelw run //max/kernels/test/gpu/linalg:nvfp4_w4a4_tiled_test.mojo.test`
   (run, not test) then
   `./bazelw test //max/tests/integration/nn:cuda_nvfp4_tests --test_output=all --nocache_test_results --test_arg=-k --test_arg=w4a4`
5. Perf: `./bazelw run //max/kernels/benchmarks:gpu/linalg/bench_nvfp4_w4a4`
   (lanes: naive w4a4 / tiled / bf16-flush / bf16-resid; watch `tiled/bf16f`).
6. End-to-end A/B: `bash /root/rerun_w4a4_tiled.sh` (true-FP4) and
   `bash /root/run_bf16_timed.sh` (bf16), then
   `grep -E "OneShotScheduler: (Starting|Completed)" /root/serve_*.log`.
7. #22 rung 1 (cp.async): mirror the double-buffer structure from
   `nvfp4_w4a16_fused_cuda.mojo` (`_load_strip`/commit-group/wait-group pattern,
   NUM_STAGES=2) onto `_gemm_kernel_tiled`'s staging loops.

## References
- Renders: `/Users/mardel/nvfp4-w4a4-renders/` (+ `compare.html`, 4-variant).
- Memories: `nvfp4-fused-d1-d2-status`, `mojo-devicebuffer-harness-lifetime`,
  `mojo-gpu-kernel-idioms`, `max-build-box-nvfp4`, `sm120-native-fp4-mma`.
- Prior checkpoint: `checkpoints/2026-07-05-1224-checkpoint.md`.
